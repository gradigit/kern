# Kern release gate

This file is a compatibility alias.

Use [Release validation gate](release-validation-gate.md) as the canonical public definition of:
- contributor CI baseline
- maintainer release-only validation
- current unsigned DMG release posture
